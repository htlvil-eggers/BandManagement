Ein Benutzer ist vorhanden:
Username: eggers
Password: eggers
Bandname: edelBlech

Mitglieder zu einer Band kann man nur über den C#-Client hinzufügen (wurde so gelöst, da wir aktuell noch keine Lounge geplant haben, in der man sich eine Band suchen kann, sondern der Bandleader legt die Zugangsdaten an und die Mitglieder können sich dann einloggen und sie ändern). Das Hinzufügen von Mitglieder wurde allerdings noch nicht implementiert. Was möglich ist, ist eine Band anzulegen (der Bandleader wird da gleich mitangelegt).

Der implementierte Kalender wurde beispielhaft in den JavaClient unter seinen zukünftigen Platz eingefügt und mit Beispieldaten initialisiert. Es ist möglich mehrere Tage zu markieren und die Markierung ist nur im angegebenen Intervall möglich (im Beispiel von 10.11.2015 bis 10.12.2015)
